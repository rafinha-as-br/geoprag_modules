import 'package:flutter/material.dart';

class DashboardDeDistribuicoesScreen extends StatelessWidget {
  const DashboardDeDistribuicoesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DashboardDeDistribuicoesScreen')),
      body: const Center(
        child: Text('Tela DashboardDeDistribuicoesScreen (Mock)'),
      ),
    );
  }
}
