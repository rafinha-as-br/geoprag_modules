import 'package:flutter/material.dart';

class DashboardDeAplicadoresScreen extends StatelessWidget {
  const DashboardDeAplicadoresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('DashboardDeAplicadoresScreen')),
      body: const Center(
        child: Text('Tela DashboardDeAplicadoresScreen (Mock)'),
      ),
    );
  }
}
