import 'package:flutter/material.dart';

class VisualizacaoDeCorregoScreen extends StatelessWidget {
  const VisualizacaoDeCorregoScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('VisualizacaoDeCorregoScreen')),
      body: const Center(
        child: Text('Tela VisualizacaoDeCorregoScreen (Mock)'),
      ),
    );
  }
}
