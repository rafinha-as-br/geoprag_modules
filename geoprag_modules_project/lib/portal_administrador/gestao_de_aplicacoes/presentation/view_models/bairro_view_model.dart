/// View model de bairro para o módulo `gestao_de_aplicacoes` — scaffolding
/// original da feat/GEOPRAG-41, ainda sem uso definido (nenhuma tela deste
/// módulo consome esta classe hoje; ver `AplicadorOpcaoViewModel` e
/// `PontoDeAplicacaoResumoViewModel` para os view models realmente em uso).
class BairroViewModel {
  final String id;

  const BairroViewModel({required this.id});
}
