import 'package:flutter/material.dart';

/// Exibe o mapa de gaspar com os bairros clicáveis, com cada bairro mostrando uma prévia da cobertura de aplicações
class MapaGasparWidget extends StatelessWidget {
  const MapaGasparWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
